# 合成数据跑通项目

文档版本：v1.2  
编制日期：2026-09-01  
项目目录：`D:\风险定量评估\qra-platform`  
当前状态：M1.5 全 361 字段离线合同链及既有 69 字段阿里云百炼在线样本已验收；生产正式 QRA 仍受门禁  
状态回填日期：2026-09-02  
状态说明：文档中的 163 个实施项均已回填为 `[x]`；361 字段按 256 个直接证据项目事实、4 个结构派生项目事实、68 个参数和 33 个运行假设闭合。  
目标版本：QRA 全合成原始资料端到端演示版 v1

---

## 1. 文档目的

本文件用于指导项目在真实工程数据尚未完整、项目是否正式实施尚未确定的情况下，先使用人工合成数据跑通完整软件流程。

本项目不是重新开发一套系统，而是在现有代码基础上，把已经分别完成的资料处理、风险计算和报告输出能力连接为一条普通用户可操作、可重复验收、可逐步替换真实数据的完整产品链路。

目标链路为：

```text
用户新建项目
→ 上传合成的 Excel、CSV、DOCX、PDF、图片等原始资料
→ 系统自动执行文件检查、分类、解析、OCR 和大模型提取
→ 系统完成字段标准化、单位换算、实体对齐、冲突检测
→ 用户只处理低置信度、冲突和缺失问题
→ 系统生成不可变输入快照
→ 自动执行 11/11 风险计算节点
→ 自动形成图表、结果解释和报告草稿
→ 人工确认并导出带有合成数据声明的测试报告
```

本文件应作为后续分阶段实现、验收、复盘和状态更新的主执行清单。

---

## 2. 核心结论

当前项目已经存在两个技术闭环：

1. 原始资料可以经过解析、提取、人工复核和快照，进入筛查计算，但当前内部验收只覆盖 5 个目标节点。
2. 完整合成 JSON 可以执行全部 11 个计算节点并生成可视化报告，但这条路径绕过了原始资料识别和复核环节。

因此，本项目的核心任务不是继续孤立开发更多算法或页面，而是补齐两条链路之间的连接：

> 以现有完整合成 JSON 为标准答案，反向生成多格式合成原始资料，再让这些原始资料经过第一部分形成等价输入快照，最终完成 11/11 计算和受控报告。

本项目里程碑命名为：

> M1.5：全合成原始资料到完整 QRA 测试报告闭环

---

## 3. 当前基线

### 3.1 已有能力

#### 第一部分：资料理解和输入治理

当前已经具备：

- 多文件上传和文件安全检查；
- CSV、XLS、XLSX、DOCX、PDF、图片等资料处理能力；
- 原生文本解析、扫描页 OCR、大图压缩和切片；
- 大模型字段提取接口；
- 版本化字段合同和映射配置；
- 单位归一、字段匹配、实体融合和冲突检测；
- 候选字段、来源文件和证据位置绑定；
- 三栏式人工复核工作台；
- 接受、修改、替换来源、驳回、不适用和重新提取；
- 服务端质量门禁；
- 不可变输入快照和全过程审计。

#### 第二部分：风险计算引擎

当前已经具备：

- 数据清单和工程指标覆盖率；
- 管段几何；
- 分机理失效频率；
- 泊松失效概率；
- 泄漏孔径和泄漏点离散；
- AQ/T 3046 源项和后果公式链；
- 喷射火热辐射阈值；
- GB/T 34346 附录 C；
- 自适应证据风险评价；
- 人员 IR、F-N、PLL；
- 管段风险排序和风险矩阵；
- 动态节点跳过、隔离失败和补数清单；
- 结构化结果、数值哈希和可重复运行。

#### 第三部分：结果和报告

当前已经具备：

- JSON、CSV、SVG、HTML 和 ZIP 输出；
- 管段排名、风险矩阵和图表；
- 数据完整度、节点状态和正式发布阻断项；
- 合成场景对比报告；
- 数据库报告资源和浏览器查看入口。

目前报告主要是确定性模板，还没有形成受控的大模型报告智能体。

### 3.2 当前验证结果

2026-09-01 重新执行现有项目验证：

- 架构检查：通过；
- 自动化测试：254 项成功；
- 条件跳过：1 项未启用的真实在线 OCR 测试；
- 已有 5 个完整合成计算场景；
- 每个完整合成场景均完成 11/11 节点；
- 现有合成测试报告始终保持 `formal_report_allowed=false`。

### 3.3 当前核心缺口

| 缺口 | 当前状态 | 本项目处理方式 |
|---|---|---|
| 原始资料到 11/11 完整计算 | 原始资料闭环目前只验收 5 个节点 | 建立完整合成原始资料包并扩展字段覆盖 |
| 计算参数来源混杂 | 项目事实、模型参数和场景假设容易混在一起 | 分成三类数据并分别版本化 |
| 普通用户流程分散 | 功能存在于多个管理页面 | 建立统一项目向导 |
| 报告智能体 | 目前以确定性模板为主 | 增加受控结构化报告智能体 |
| 在线模型不稳定 | 自动化验收不能依赖公网模型 | 同时支持确定性回放和在线模型两种模式 |
| 正式工程数据缺失 | 无法完成正式 QRA | 演示版持续阻断正式签发，不影响软件闭环 |

---

## 4. 项目目标与非目标

### 4.1 总目标

实现一套不依赖真实项目数据的完整演示系统。用户只需要选择演示项目或上传合成原始资料，系统即可完成资料理解、复核、计算和报告。

### 4.2 分目标

1. 建立覆盖全部 11 个计算节点的合成项目数据。
2. 把标准合成 JSON 转换为多格式原始资料，而不是直接导入 JSON。
3. 证明第一部分提取出的快照与标准答案一致。
4. 证明同一快照可以稳定产生相同计算结果。
5. 证明报告中的数字、图表和结论能够反查计算结果与输入证据。
6. 形成普通用户可操作的统一项目流程。
7. 保留未来把合成资料逐项替换为真实资料的接口。

### 4.3 本阶段明确不做

- 不宣称任何合成结果代表真实管线风险；
- 不解除正式报告阻断；
- 不自行批准失效频率库、修正系数或风险准则；
- 不把大模型用于数值计算；
- 不让大模型修改 PLL、IR、F-N、失效频率或后果数值；
- 不追求第一版识别任意格式、任意行业、任意客户资料；
- 不建设微服务、分布式事务或复杂消息队列；
- 不将当前测试报告包装为正式工程报告；
- 不把缺失字段自动转换为 0；
- 不把 0～100 综合分数作为正式 QRA 的替代结果。

---

## 5. 产品分级与发布边界

系统以后统一划分为三个产品等级。

| 等级 | 输入数据 | 计算能力 | 报告 | 发布边界 |
|---|---|---|---|---|
| DEMO_SYNTHETIC | 完全合成 | 允许 11/11 | 合成测试报告 | 禁止真实资产评价 |
| REAL_SCREENING | 不完整真实资料和明确先验 | 按数据动态运行 | 筛查报告 | 禁止正式 QRA 签发 |
| FORMAL_QRA | 完整真实数据和已批准参数 | 要求 11/11 | 正式报告 | 需专家验证、审批和签发 |

本项目只交付 `DEMO_SYNTHETIC`。

所有演示输入、快照、结果和报告必须携带：

- `data_classification=SYNTHETIC_TEST_ONLY`；
- `zero_real_project_data=true`；
- `formal_report_allowed=false`；
- 允许用途；
- 禁止用途；
- 合成数据包版本；
- 合成参数包版本；
- 生成器版本和哈希。

---

## 6. 数据三层架构

这是本项目最重要的数据边界。

### 6.1 第一层：用户上传的项目事实

由第一部分从原始资料中提取，例如：

- 管线和管段；
- 起止里程、长度、管径、壁厚；
- 设计压力、运行压力和温度；
- 天然气组分；
- 防腐层、阴极保护和腐蚀观测；
- 缺陷、维修和检测记录；
- 巡线、第三方活动和地质灾害；
- 阀门、泄漏检测和关断时间；
- 人口、受体、建筑物和关键用户；
- 气象联合概率。

每个字段必须保留：

- `field_id`；
- 原始值；
- 标准化值；
- 单位；
- 来源文件；
- 页码、表格、单元格或图片坐标；
- 提取方法和模型版本；
- 置信度；
- 人工决定；
- 数据质量等级。

### 6.2 第二层：系统管理的模型和参数库

不要求用户每次上传，包括：

- 分机理基准失效频率；
- 孔径条件比例；
- 频率修正模型；
- 点火概率模型；
- 气体物性参数；
- 后果模型参数；
- 人员死亡概率模型；
- 风险准则；
- 泄漏点离散配置；
- 报告模板。

演示版建议建立：

- `synthetic-frequency-library-v1`；
- `synthetic-frequency-correction-v1`；
- `synthetic-consequence-parameters-v1`；
- `synthetic-ignition-model-v1`；
- `synthetic-risk-criteria-v1`。

模型参数必须记录版本和状态，不得伪装成从用户资料中提取的项目事实。

### 6.3 第三层：本次运行的场景假设和人工决定

例如：

- 是否允许使用合成先验；
- 选择哪个冲突候选；
- 手工填写的字段；
- 单位修正；
- 不适用项；
- 气象或人口场景；
- 计算目标节点；
- 报告用途。

这些内容必须进入不可变快照或运行配置，并保留决定人、原因、时间和哈希。

---

## 7. 目标业务流程

### 7.1 普通用户流程

```text
新建项目
→ 选择“全合成演示项目”或上传资料
→ 查看资料处理进度
→ 查看自动提取结果
→ 只处理系统标出的待复核项
→ 查看可运行节点和数据缺口
→ 确认输入快照
→ 自动创建计算任务
→ 查看 11 个节点进度
→ 查看结果、图表和报告草稿
→ 确认并导出测试报告
```

### 7.2 建议统一状态

| 状态 | 含义 |
|---|---|
| PROJECT_CREATED | 项目已创建 |
| FILES_UPLOADED | 资料已上传 |
| PARSING | 正在解析和 OCR |
| EXTRACTING | 正在执行字段提取 |
| REVIEW_REQUIRED | 存在待复核项 |
| READY_TO_CONFIRM | 服务端门禁通过 |
| SNAPSHOT_CONFIRMED | 不可变输入快照已创建 |
| CALCULATING | 正在运行计算节点 |
| REPORT_DRAFTED | 报告草稿已生成 |
| REPORT_REVIEW_REQUIRED | 报告等待人工确认 |
| COMPLETED | 测试流程已完成 |
| BLOCKED | 存在阻断问题 |
| FAILED | 系统执行失败 |

### 7.3 智能体边界

#### 可以使用大模型的地方

- 文件分类辅助；
- OCR 后文本理解；
- 文档、表格和图片字段提取；
- 字段语义匹配；
- 冲突说明；
- 报告摘要和结果解释；
- 数据缺口和补数建议。

#### 必须使用确定性程序的地方

- 文件安全检查；
- 单位换算；
- 合同和 Schema 校验；
- 冲突门禁；
- 快照哈希；
- 失效频率和概率计算；
- 后果计算；
- IR、F-N、PLL 和排序；
- 数字、表格和图表；
- 报告数字一致性检查；
- 正式发布门禁。

---

## 8. 目标计算节点与数据来源

| 序号 | 节点 | 项目事实 | 系统参数 | 主要输出 |
|---:|---|---|---|---|
| 1 | data_inventory | 上传资料和字段统计 | 数据合同 | 数据清单、缺口 |
| 2 | indicator_coverage | 工程观测 | 指标目录 | 覆盖率和质量 |
| 3 | segment_geometry | 管段、里程、管径、壁厚、坐标 | 几何规则 | 计算管段 |
| 4 | failure_frequency | 管段和工程指标 | 基准频率、孔径比例、修正模型 | 频率和失效概率 |
| 5 | leak_point_discretization | 管段和阀室信息 | 离散配置 | 泄漏点 |
| 6 | aqt3046_source_term | 压力、温度、气质、管径、壁厚 | 物性和源项模型 | 泄漏源项 |
| 7 | jet_fire_thresholds | 源项和场景 | 热辐射参数 | 阈值距离 |
| 8 | gbt34346_annex_c | 管段和隐患数据 | 标准公式配置 | 隐患分级结果 |
| 9 | adaptive_evidence_qra | 工程指标和证据质量 | 先验和证据模型 | 条件化筛查风险 |
| 10 | human_qra | 人口、受体、气象、点火条件 | 后果和人员模型 | IR、F-N、PLL |
| 11 | risk_matrix | 频率和后果结果 | 风险准则 | 矩阵和等级 |

阶段 1 必须把每个节点的全部必需字段进一步展开为逐字段矩阵。

---

## 9. 合成数据总体设计

### 9.1 标准答案来源

以现有文件作为初始标准答案：

`qra-platform/tests/fixtures/qra_synthetic_case_v1.json`

以现有 5 个计算场景作为计算响应基线：

- S00_BASELINE：基准场景；
- S10_CORROSION_DEGRADATION：腐蚀恶化；
- S20_THIRD_PARTY_SURGE：第三方活动激增；
- S30_HIGH_PRESSURE_POPULATION_PEAK：高压力与人口峰值；
- S40_MITIGATION_PACKAGE：综合治理措施。

S00 基准场景已完成完整原始资料闭环；S10 至 S40 也已按同一格式批量生成并通过 4 个场景、40 份原始资料、每场景 256 项直接证据和 11/11 全合同数值哈希验收。机器记录见 `resources/synthetic/full-chain-v1/stage2/extended-scenarios-acceptance.json`。

### 9.2 合成原始资料包

建议第一版生成以下资料：

| 文件 | 格式 | 主要内容 | 识别方式 |
|---|---|---|---|
| 01_管线与管段台账 | XLSX | 管段、里程、管径、壁厚、压力等级 | 表格解析 |
| 02_运行工况 | CSV | 压力、温度、检测和关断时间 | 表格解析 |
| 03_天然气组分 | XLSX | 气体组成和摩尔分数 | 表格解析 |
| 04_CIPS与腐蚀检测 | XLSX | 保护电位、缺陷密度、腐蚀速率 | 表格解析 |
| 05_缺陷与维修记录 | DOCX | 缺陷、维修状态和日期 | 文档解析和大模型 |
| 06_第三方与巡线记录 | XLSX | 开挖活动、巡线周期和管理指标 | 表格解析 |
| 07_人口和敏感受体 | XLSX | 昼夜人数、位置、学校、工业点 | 表格解析 |
| 08_气象联合概率 | CSV | 稳定度、风速、风向、昼夜概率 | 表格解析 |
| 09_现场检查扫描件 | PDF | 合成检查表和签字页 | OCR 和大模型 |
| 10_现场照片说明 | PNG/JPEG | 合成标牌、缺陷说明或仪表读数 | 视觉 OCR |

第一版不追求文件数量多，而要保证每种处理路径都有代表性，同时覆盖全部关键字段。

### 9.3 建议目录

```text
qra-platform/
├─ resources/
│  └─ synthetic/
│     └─ full-chain-v1/
│        ├─ manifest.schema.json
│        ├─ parameter-pack.schema.json
│        ├─ report-context.schema.json
│        └─ README.md
├─ tests/
│  └─ fixtures/
│     └─ full_chain_synthetic_v1/
│        ├─ canonical-case.json
│        ├─ expected-snapshot.json
│        ├─ expected-results.json
│        ├─ field-source-matrix.csv
│        └─ document-variants.json
├─ tools/
│  ├─ build_synthetic_source_pack.py
│  ├─ run_full_chain_synthetic_demo.py
│  └─ validate_full_chain_synthetic_demo.py
└─ workspace/
   ├─ inputs/
   │  └─ QRA全合成原始资料包_v1/
   └─ outputs/
      └─ QRA全合成端到端演示版_v1/
```

生成器和合同进入版本控制；完整运行资料和产物进入 `workspace`。小型、完全合成、可复现的黄金夹具可以进入 `tests/fixtures`。

### 9.4 原始资料条件变体

计算场景和资料质量场景必须分开。

建议资料条件使用以下 ID：

| ID | 场景 | 预期行为 |
|---|---|---|
| D00_CLEAN | 完整、清晰、无冲突 | 快速进入确认 |
| D10_CONFLICT | 同一字段在两份资料中冲突 | 必须人工选择 |
| D20_MISSING | 缺少人口、气象或气质 | 11/11 不得伪通过 |
| D30_LOW_QUALITY_SCAN | 模糊、旋转、低对比度扫描件 | 触发预处理、低置信度和复核 |
| D40_OVERSIZED_IMAGE | 超大或超长图片 | 自动压缩或切片并回映坐标 |
| D50_PROMPT_INJECTION | 文档中含有诱导模型改变流程的文字 | 只能作为普通文档内容，不得执行 |
| D60_DUPLICATE_VERSION | 同名资料多个版本 | 显式显示版本和重复关系 |
| D70_UNIT_ANOMALY | MPa、kPa、mm、m 等单位混用 | 确定性归一或阻断 |

第一条完整验收链为：

`S00_BASELINE × D00_CLEAN`。

负向验收至少覆盖 D10、D20、D30、D40 和 D50。

### 9.5 黄金答案要求

每个合成字段必须在清单中记录：

- 标准字段 ID；
- 标准值和单位；
- 出现的源文件；
- 页码、表格、单元格或图片区域；
- 是否关键字段；
- 是否允许缺失；
- 是否应该产生冲突；
- 影响的计算节点；
- 预期候选数量；
- 预期质量问题；
- 预期人工决定；
- 最终快照路径和值。

合成资料生成不得使用不可复现的随机数。若需要扰动，应使用固定规则和明确版本。

---

## 10. 两种模型运行模式

### 10.1 确定性验收模式

标识：`SYNTHETIC_DETERMINISTIC`

用途：

- 单元测试；
- 集成测试；
- CI 回归；
- 离线演示；
- 快照和结果哈希验证。

要求：

- 不访问公网模型；
- OCR 和字段提取使用版本化夹具或回放结果；
- 同一输入必须产生相同候选、快照和结果哈希；
- 必须验证真实的业务编排和门禁，不能直接绕过第一部分。

### 10.2 在线大模型演示模式

标识：`SYNTHETIC_LIVE_MODEL`

用途：

- 展示真实 OCR 和大模型提取；
- 验证提示词、模型版本和重试；
- 收集合成黄金集准确率。

要求：

- 记录模型提供方、模型 ID、调用时间和调用审计；
- 不记录密钥和完整敏感请求；
- 在线模型输出仍须通过相同 Schema 和人工门禁；
- 模型不可用时保留解析结果并允许人工处理；
- 在线模式的文字和置信度可以变化，但业务合同不能变化。

正式验收以确定性模式为必过项，在线模型模式作为单独的能力验收，不能让公网模型波动阻断基础回归。

---

## 11. 分阶段实施计划

# 阶段 0：冻结基线和完成定义

建议工期：1～2天  
阶段目标：明确本项目的边界、验收口径和可回退基线。

### 11.1 工作任务

- [x] S0-01 保存当前 Git 状态、依赖和架构检查结果。
- [x] S0-02 保存当前 254 项测试结果。
- [x] S0-03 记录当前完整合成测试版的 5 个数值哈希。
- [x] S0-04 记录当前数据库 Schema 版本。
- [x] S0-05 确认目标只为 `DEMO_SYNTHETIC`。
- [x] S0-06 固定第一条验收链为 `S00_BASELINE × D00_CLEAN`。
- [x] S0-07 固定 11 个目标节点。
- [x] S0-08 固定“不得直接上传标准 JSON 代替原始资料”的验收规则。
- [x] S0-09 固定所有报告均阻断正式签发。
- [x] S0-10 建立本项目的决策记录和风险登记册。

### 11.2 交付物

- 基线版本记录；
- 测试结果记录；
- 合成结果哈希记录；
- 范围和非范围说明；
- M1.5 完成定义；
- 决策记录；
- 风险登记册。

### 11.3 阶段验收

- [x] 新目录或新环境可以启动当前系统。
- [x] 现有 254 项测试无回归失败。
- [x] 5 个现有合成场景仍为 PASS。
- [x] 项目成员对“演示可用不等于正式QRA可用”没有歧义。

### 11.4 阶段门

状态建议：

- `S0_NOT_STARTED`
- `S0_IN_PROGRESS`
- `S0_BASELINE_FROZEN`

只有 `S0_BASELINE_FROZEN` 后进入阶段 1。

---

# 阶段 1：完整字段合同和节点覆盖矩阵

建议工期：2～4天  
阶段目标：明确 11 个节点的所有输入从哪里来。

### 11.5 工作任务

- [x] S1-01 导出当前 `qra-input` 合同全部字段。
- [x] S1-02 导出 11 个动态节点的必需输入和可选输入。
- [x] S1-03 为每个字段标记数据层：项目事实、系统参数或场景假设。
- [x] S1-04 为项目事实分配合成原始文件。
- [x] S1-05 为系统参数分配合成参数包。
- [x] S1-06 明确每个字段的标准单位。
- [x] S1-07 标记关键字段、阻断字段和允许缺失字段。
- [x] S1-08 标记每个字段影响的节点。
- [x] S1-09 标记证据位置要求。
- [x] S1-10 标记可用确定性解析还是需要大模型。
- [x] S1-11 识别当前映射、提取 Schema 和复核组装的覆盖缺口。
- [x] S1-12 建立字段覆盖率自动检查。

### 11.6 交付物

核心交付物：`field-source-node-matrix.csv`。

至少包含以下列：

| 列 | 含义 |
|---|---|
| field_id | 标准字段 ID |
| business_name | 业务名称 |
| target_path | 输入快照路径 |
| data_layer | PROJECT_FACT / MODEL_PARAMETER / RUN_ASSUMPTION |
| source_document | 合成源资料 |
| source_location_type | cell/page/bbox/paragraph/system |
| source_unit | 原始单位 |
| target_unit | 标准单位 |
| criticality | BLOCKING/IMPORTANT/OPTIONAL |
| extraction_method | deterministic/ocr/llm/manual/system |
| target_nodes | 影响节点 |
| missing_policy | 缺失处理 |
| conflict_policy | 冲突处理 |
| evidence_required | 是否必须绑定证据 |

### 11.7 阶段验收

- [x] 11 个节点的所有必需输入都有来源。
- [x] 不存在“为了让节点通过而默认补值但未登记”的字段。
- [x] 所有项目事实都能分配到原始资料。
- [x] 所有模型参数都能分配到版本化参数包。
- [x] 所有场景假设都需要明确记录。
- [x] 覆盖检查可以自动发现新增节点或字段遗漏。

### 11.8 阶段门

通过状态：`S1_FULL_CONTRACT_MAPPED`。

---

# 阶段 2：合成原始资料和参数包生成器

建议工期：4～7天  
阶段目标：从标准合成案例稳定生成多格式原始资料和黄金答案。

### 11.9 工作任务

- [x] S2-01 建立合成数据包 Manifest Schema。
- [x] S2-02 建立合成参数包 Schema。
- [x] S2-03 开发 `build_synthetic_source_pack.py`。
- [x] S2-04 从 S00 标准 JSON 读取项目事实。
- [x] S2-05 生成管段、运行工况、气质、CIPS、人口和气象表格。
- [x] S2-06 生成 DOCX 缺陷和维修记录。
- [x] S2-07 生成至少一份扫描 PDF。
- [x] S2-08 生成至少一张合成图片。
- [x] S2-09 在所有文件中写入明显的“合成测试数据”标识。
- [x] S2-10 生成 `ground-truth.json`。
- [x] S2-11 生成字段到证据位置的 Manifest。
- [x] S2-12 生成合成参数库。
- [x] S2-13 生成预期快照。
- [x] S2-14 生成预期计算结果和哈希。
- [x] S2-15 生成 D10 至 D70 资料条件变体。
- [x] S2-16 验证生成过程无随机漂移。

### 11.10 数据生成规则

- 同一生成器版本和输入必须生成相同业务内容；
- 日期、ID 和文件名使用固定规则；
- 二进制文件若存在元数据时间漂移，业务内容哈希和标准化哈希必须稳定；
- 每份资料必须携带 `SYNTHETIC_TEST_ONLY`；
- 合成内容不得使用客户真实事实；
- 可以依据标准文档确定字段和单位，但不能把标准示例值当作项目实测值；
- 可以依据技术方案确定资料形态和章节结构，但所有项目数值必须来自合成案例；
- 缺失、冲突和异常必须由生成器显式注入。

### 11.11 交付物

- 合成资料生成器；
- S00 完整原始资料包；
- D00 至 D70 条件变体；
- 合成参数包；
- 黄金答案；
- 证据 Manifest；
- 预期快照；
- 预期结果；
- 生成器测试。

### 11.12 阶段验收

- [x] S00×D00 能生成全部预定文件。
- [x] 所有关键字段都能在原始资料中找到。
- [x] 所有原始资料值与黄金答案一致。
- [x] 生成器连续运行两次，业务哈希一致。
- [x] D10 确实包含可验证冲突。
- [x] D20 确实缺少指定关键输入。
- [x] D30、D40 文件能够被系统安全接收。
- [x] D50 的诱导文字不会被生成器当作业务字段。

### 11.13 阶段门

通过状态：`S2_SYNTHETIC_SOURCE_PACK_ACCEPTED`。

---

# 阶段 3：原始资料到不可变快照

建议工期：5～10天  
阶段目标：让合成原始资料真正经过第一部分，形成与标准答案一致的输入快照。

### 11.14 工作任务

- [x] S3-01 为合成资料建立专用版本化映射。
- [x] S3-02 扩展字段提取 Schema。
- [x] S3-03 扩展字段别名、单位和枚举规则。
- [x] S3-04 扩展表格、DOCX、PDF 和图片证据定位。
- [x] S3-05 扩展实体和关系提取。
- [x] S3-06 扩展同一管段跨文件对齐。
- [x] S3-07 扩展项目事实与系统参数的组装边界。
- [x] S3-08 扩展复核工作台字段分组和计算节点影响提示。
- [x] S3-09 实现确定性回放模式。
- [x] S3-10 实现在线大模型演示模式。
- [x] S3-11 实现黄金答案差异检查。
- [x] S3-12 实现转换结果覆盖率报告。
- [x] S3-13 实现缺失字段不转零检查。
- [x] S3-14 实现提示注入不改变工作流检查。
- [x] S3-15 确认后生成不可变快照和完整来源链。

### 11.15 关键实现要求

1. 原始资料不得直接写入计算数据库。
2. 大模型输出只能成为候选，不能直接成为已确认事实。
3. 无证据的文档候选必须拒绝。
4. 同一字段不同值必须形成显式冲突。
5. 关键低置信字段必须人工确认。
6. 系统参数必须显示为参数库来源，不得伪造文件证据。
7. 人工修改必须保留原始值、修改值、原因和人员。
8. 快照创建后不得原地修改。
9. 相同输入和相同复核决定必须得到相同业务哈希。

### 11.16 交付物

- 合成资料专用映射；
- 扩展后的提取合同；
- 确定性回放提供方；
- 在线大模型提供方配置；
- 快照差异检查工具；
- S00×D00 端到端转换测试；
- D10、D20、D30、D40、D50 负向测试。

### 11.17 阶段验收

#### 确定性模式

- [x] 关键字段精确率 100%。
- [x] 关键字段召回率 100%。
- [x] 证据绑定率 100%。
- [x] 无证据候选 0。
- [x] 空白转零违规 0。
- [x] 未声明默认值 0。
- [x] 提示注入改变工作流 0。
- [x] 快照与 `expected-snapshot.json` 完全一致。

#### 在线模型模式

- [x] 关键字段精确率不低于 95%（2026-09-02 百炼 `qwen3.8-max` 实测 100%，69/69）。
- [x] 关键字段召回率不低于 90%（2026-09-02 百炼 `qwen3.8-max` 实测 100%，69/69）。
- [x] 关键字段最终证据绑定率 100%（在线返回 69/69 均通过证据 ID 白名单校验）。
- [x] 所有冲突经过人工确认（D10 运行压力冲突选择 `BASE`，状态 `HUMAN_CONFIRMED`）。
- [x] 模型失败时保留解析产物和人工入口。

### 11.18 阶段门

通过状态：`S3_RAW_TO_SNAPSHOT_PASS`。

---

# 阶段 4：不可变快照到 11/11 计算

建议工期：3～5天  
阶段目标：使用阶段 3 产生的快照完成全部计算节点。

### 11.19 工作任务

- [x] S4-01 把系统参数包绑定到计算任务。
- [x] S4-02 把运行假设写入计算任务配置。
- [x] S4-03 完成快照到引擎输入合同适配。
- [x] S4-04 检查 11 个节点的可运行能力。
- [x] S4-05 自动创建计算任务。
- [x] S4-06 运行全部 11 个节点。
- [x] S4-07 比较原始资料链结果与现有直接 JSON 结果。
- [x] S4-08 检查频率守恒、概率归一和场景分支守恒。
- [x] S4-09 检查数值哈希可重复性。
- [x] S4-10 检查所有结果都能反查输入快照、参数包和模型版本。
- [x] S4-11 对 D20 缺失资料验证节点跳过和补数清单。
- [x] S4-12 保持正式报告闸门关闭。

### 11.20 结果一致性规则

对于 S00×D00：

- 管段数量必须一致；
- 年失效频率必须一致；
- 一年内至少一次失效概率必须一致；
- PLL 必须一致；
- 最大 IR 必须一致；
- F-N 曲线必须一致；
- 风险最高管段必须一致；
- 11 个节点状态必须全部为 `COMPLETED`；
- 数值结果哈希必须与预期一致，或在明确的版本变更记录中更新。

### 11.21 交付物

- 合成参数包绑定机制；
- 快照到 11/11 计算集成测试；
- 结果差异报告；
- 守恒检查报告；
- 确定性复跑记录；
- 完整反向追溯记录。

### 11.22 阶段验收

- [x] S00×D00 完成 11/11。
- [x] 节点失败 0。
- [x] 节点跳过 0。
- [x] 计算结果与基线一致。
- [x] 相同快照重复计算，数值哈希一致。
- [x] D20 不得完成被缺失数据阻断的节点。
- [x] D20 报告中的缺失值不得显示为 0。
- [x] 所有测试结果保持 `formal_report_allowed=false`。

### 11.23 阶段门

通过状态：`S4_FULL_11_OF_11_SYNTHETIC_PASS`。

达到该状态即完成 M1.5 的计算主链。

---

# 阶段 5：统一项目向导和普通用户体验

建议工期：3～5天  
阶段目标：普通用户不需要理解转换任务、快照和节点内部原理。

### 11.24 页面设计

#### 页面一：项目列表

显示：

- 项目名称；
- 数据类型；
- 当前状态；
- 数据完整度；
- 待处理问题；
- 计算进度；
- 最新报告。

提供：

- 新建项目；
- 加载全合成演示项目；
- 进入项目；
- 归档项目。

#### 页面二：资料上传

显示：

- 支持的资料类型；
- 已上传文件；
- 文件安全和解析状态；
- 重复、版本和隔离信息；
- 上传后的自动处理进度。

#### 页面三：数据复核

复用现有三栏工作台，增加：

- 按计算节点筛选；
- 按关键程度筛选；
- 显示字段影响；
- 显示数据层；
- 显示系统参数包；
- 显示距离确认还有多少阻断项。

#### 页面四：计算进度

显示：

- 11 个节点；
- 每个节点状态；
- 使用的数据和参数版本；
- 跳过或失败原因；
- 预计补数项；
- 结果摘要。

#### 页面五：报告中心

显示：

- 报告草稿；
- 数据完整度；
- 数字一致性检查；
- 引用检查；
- 人工确认状态；
- HTML、PDF、DOCX 或 ZIP 导出入口。

### 11.25 工作任务

- [x] S5-01 建立项目级聚合页面。
- [x] S5-02 增加“加载演示项目”入口。
- [x] S5-03 将上传、转换、复核、快照、计算和报告串成一个向导。
- [x] S5-04 统一状态和进度显示。
- [x] S5-05 隐藏普通用户不需要的 JSON 和数据库概念。
- [x] S5-06 保留高级审计入口。
- [x] S5-07 增加合成数据醒目标识。
- [x] S5-08 增加失败后的重试和继续处理入口。
- [x] S5-09 验证桌面和窄屏布局。
- [x] S5-10 验证键盘访问和错误提示。

### 11.26 阶段验收

- [x] 用户无需命令行即可完成流程。
- [x] 用户无需粘贴 JSON。
- [x] 用户只需要处理被标出的复核项。
- [x] 用户能够理解当前为什么被阻断。
- [x] 用户能够从最终报告回到项目、计算和输入资料。
- [x] 全流程不存在只能从数据库页面完成的普通业务操作。

### 11.27 阶段门

通过状态：`S5_USER_JOURNEY_ACCEPTED`。

---

# 阶段 6：受控报告智能体

建议工期：5～7天  
阶段目标：由大模型形成可读的报告解释，但所有数字和结论边界仍由程序控制。

### 11.28 报告架构

```text
不可变输入快照
      +
不可变计算结果
      +
节点状态、参数版本、证据索引
      ↓
report-context-v1.json
      ↓
大模型生成结构化 report-draft-v1.json
      ↓
确定性数字、引用和禁用表述检查
      ↓
确定性图表和排版
      ↓
人工复核
      ↓
测试报告
```

### 11.29 Report Context 最小内容

- 项目标识；
- 数据分类；
- 输入快照 ID 和哈希；
- 计算任务 ID 和数值哈希；
- 模型和参数版本；
- 11 个节点状态；
- 数据完整度；
- 关键风险指标；
- 管段排名；
- F-N 曲线；
- 风险矩阵；
- 图表资源；
- 证据索引；
- 缺失数据；
- 不确定性；
- 正式报告阻断项。

### 11.30 Report Draft 最小结构

- `section_id`；
- `paragraph_id`；
- `text_template`；
- `metric_refs`；
- `evidence_refs`；
- `result_refs`；
- `uncertainty_refs`；
- `prohibited_claim_check`。

大模型尽量引用指标 ID，由排版程序填入最终数字。不得让模型在自由文本中重新计算或改写数值。

### 11.31 报告章节

1. 项目概述；
2. 合成数据和使用边界；
3. 输入资料与数据完整度；
4. 模型和参数说明；
5. 失效频率和失效概率；
6. 后果分析；
7. 个人风险 IR；
8. 社会风险 F-N 和 PLL；
9. 管段风险排序；
10. 风险驱动因素；
11. 场景对比；
12. 不确定性和数据缺口；
13. 建议措施；
14. 正式发布阻断项；
15. 输入、模型、结果和证据引用目录。

### 11.32 工作任务

- [x] S6-01 定义 `report-context-v1`。
- [x] S6-02 定义 `report-draft-v1`。
- [x] S6-03 实现结果到报告上下文的确定性转换。
- [x] S6-04 实现报告生成提示和结构化输出。
- [x] S6-05 实现数字引用检查。
- [x] S6-06 实现证据引用检查。
- [x] S6-07 实现节点状态一致性检查。
- [x] S6-08 实现禁用表述检查。
- [x] S6-09 实现合成数据水印。
- [x] S6-10 实现大模型不可用时的确定性模板降级。
- [x] S6-11 实现报告人工确认。
- [x] S6-12 保存报告版本、上下文哈希和草稿哈希。
- [x] S6-13 输出 HTML。
- [x] S6-14 根据交付需要补充 PDF 或 DOCX。

### 11.33 禁止大模型执行的行为

- 修改或重算任何风险数字；
- 删除不利场景；
- 隐藏缺失数据；
- 把筛查结果写成正式结论；
- 自行认定风险可接受；
- 自行解除发布阻断；
- 自行补充工程事实；
- 伪造标准条款或证据引用。

### 11.34 阶段验收

- [x] 报告所有数字与计算结果一致。
- [x] 所有关键结论都有结果或证据引用。
- [x] 不存在无来源的项目事实。
- [x] 缺失节点不会被写成已完成。
- [x] 合成报告不会使用“正式评价通过”等表述。
- [x] 大模型不可用时仍能输出模板报告。
- [x] 相同上下文允许文字不同，但数字、图表和引用目标不变。

### 11.35 阶段门

通过状态：`S6_CONTROLLED_REPORT_AGENT_ACCEPTED`。

---

# 阶段 7：端到端验收和演示版本发布

建议工期：3～5天  
阶段目标：形成可以反复安装、演示、测试和交付的版本。

### 11.36 必测场景

#### 场景 A：完整正常资料

输入：S00×D00。  
预期：

- 原始资料全部接收；
- 形成候选和证据；
- 门禁通过；
- 快照与黄金答案一致；
- 11/11 完成；
- 形成报告；
- 正式发布仍被阻断。

#### 场景 B：字段冲突

输入：S00×D10。  
预期：

- 自动识别冲突；
- 未人工决定前不能确认；
- 人工选择后形成新决定哈希；
- 能继续完成 11/11；
- 报告记录复核决定。

#### 场景 C：关键数据缺失

输入：S00×D20。  
预期：

- 缺失关键字段显式显示；
- 受影响节点跳过；
- 不将缺失值写为 0；
- 输出补数清单；
- 只能形成不完整筛查报告。

#### 场景 D：低质量扫描

输入：S00×D30。  
预期：

- 执行图像预处理；
- 低置信字段进入复核；
- 证据坐标可以回到原图；
- OCR 失败不影响其他文件。

#### 场景 E：超大图片

输入：S00×D40。  
预期：

- 请求体不超过模型限制；
- 自动缩放或切片；
- 保留切片到原图的坐标映射；
- 可以对单页或单字段重新提取。

#### 场景 F：提示注入

输入：S00×D50。  
预期：

- 文档中的命令只作为不可信文本；
- 不能改变系统提示、工作流、字段合同或发布门禁；
- 相关风险进入审计。

#### 场景 G：模型不可用

预期：

- 系统明确显示模型未配置或调用失败；
- 已完成的确定性解析结果不丢失；
- 可以重试或人工处理；
- 不生成虚假的提取结果。

#### 场景 H：服务重启

预期：

- 未完成任务可恢复；
- 已确认快照不变；
- 已完成计算结果不变；
- 报告资源可继续访问。

#### 场景 I：报告数字篡改

预期：

- 报告校验器发现数字与结果不一致；
- 报告不能进入已确认状态；
- 审计记录失败原因。

### 11.37 发布交付物

- `QRA全合成端到端演示版_v1`；
- 一键启动脚本；
- 一键加载演示项目；
- 合成原始资料包；
- 合成参数包；
- 黄金答案和预期结果；
- 端到端验收工具；
- 自动化测试记录；
- 用户操作手册；
- 管理和审计手册；
- 演示脚本；
- 已知限制；
- 版本和依赖清单；
- 备份恢复说明；
- 示例测试报告。

### 11.38 最终验收标准

- [x] 从全新运行目录可以启动系统。
- [x] 不需要手工修改数据库。
- [x] 不需要手工上传标准 JSON。
- [x] 普通用户能在页面完成全流程。
- [x] S00×D00 完成 11/11。
- [x] 原始资料、候选、决定、快照、计算和报告可以双向追溯。
- [x] 结果与黄金基线一致。
- [x] 全量自动化测试通过。
- [x] 所有负向场景符合预期。
- [x] 合成数据和正式发布边界清晰。
- [x] 报告数字一致性检查通过。
- [x] 演示操作在约定环境中可重复完成。

### 11.39 最终里程碑

通过状态：

> `M1_5_FULL_SYNTHETIC_END_TO_END_ACCEPTED`

---

## 12. 自动化测试计划

### 12.1 测试层级

| 层级 | 内容 |
|---|---|
| 单元测试 | 生成器、字段归一、参数绑定、报告校验 |
| 合同测试 | Manifest、参数包、快照、报告上下文和报告草稿 |
| 集成测试 | 多格式资料到候选、复核和快照 |
| 数值回归 | 快照到 11/11 结果和哈希 |
| 端到端测试 | 上传到报告 |
| 浏览器测试 | 普通用户操作和页面错误 |
| 故障测试 | 模型失败、重启、超大文件、冲突和缺失 |
| 安全测试 | 文件入口、路径穿越、提示注入和敏感日志 |

### 12.2 建议新增测试文件

- `tests/unit/test_synthetic_source_pack.py`；
- `tests/unit/test_synthetic_parameter_pack.py`；
- `tests/unit/test_report_context.py`；
- `tests/unit/test_report_draft_validation.py`；
- `tests/integration/test_full_chain_synthetic_conversion.py`；
- `tests/integration/test_full_chain_synthetic_11_nodes.py`；
- `tests/integration/test_controlled_report_agent.py`；
- `tests/integration/test_full_chain_synthetic_browser.py`。

### 12.3 最低质量门槛

- 现有回归测试不得减少；
- 新功能必须有测试；
- 关键字段确定性模式精确率和召回率均为 100%；
- 证据绑定率 100%；
- 空白转零违规 0；
- 未登记默认值 0；
- 数值回归差异 0 或处于批准容差内；
- 报告数字不一致 0；
- 高优先级缺陷 0；
- 浏览器控制台错误 0；
- 密钥和完整模型请求泄露 0。

---

## 13. 报告和风险指标口径

### 13.1 正式 QRA 主要结果

系统应分别展示：

- 年失效频率；
- 指定期限内至少一次失效概率；
- 后果阈值距离；
- 个人风险 IR；
- 社会风险 F-N；
- 潜在生命损失 PLL；
- 管段风险贡献；
- 风险矩阵和分级；
- 数据完整度和不确定性。

### 13.2 综合风险分数

如果后续业务需要 0～100 分，应单独定义为“筛查指数”，并明确：

- 公式；
- 权重；
- 数据来源；
- 缺失处理；
- 适用范围；
- 与 IR、F-N、PLL 的关系；
- 是否经过业务批准。

不得把自定义综合分数包装成正式定量风险结果，也不得把不同风险域未经批准地加权成唯一总分。

---

## 14. 建议排期

### 14.1 单人开发建议

| 周次 | 主要工作 | 里程碑 |
|---|---|---|
| 第1周 | 阶段0、阶段1、合成资料生成器起步 | 字段和来源全部明确 |
| 第2周 | 完成合成资料包、映射和提取扩展 | 原始资料可形成候选 |
| 第3周 | 完成快照和11/11计算联调 | M1.5计算主链完成 |
| 第4周 | 用户向导和报告智能体 | 普通用户全流程完成 |
| 第5周 | 负向测试、安装包、手册和验收 | 演示版发布 |

单人稳妥周期：4～6周。

### 14.2 小组并行建议

若有 3～4 人：

- 数据/大模型：阶段1、2、3；
- 后端/计算：阶段1、4；
- 前端：阶段5；
- 报告和测试：阶段6、7。

预计 2～3周可以形成第一版内部演示，但最终仍必须通过完整端到端验收。

---

## 15. 优先级清单

### P0：必须先完成

1. 冻结 M1.5 完成定义。
2. 建立完整字段—来源—节点矩阵。
3. 建立 S00×D00 合成原始资料包。
4. 建立合成参数包。
5. 打通原始资料到标准快照。
6. 打通快照到 11/11。
7. 建立端到端自动化验收。

### P1：紧随完成

1. 统一项目向导。
2. 受控报告智能体。
3. D10 至 D50 负向场景。
4. 一键演示入口。
5. 安装和操作手册。

### P2：演示版稳定后

1. ~~扩展 S10 至 S40 原始资料包。~~ 已于 2026-09-02 完成并验收。
2. 报告 PDF 和 DOCX 输出。
3. 性能和并发优化。
4. 更多文件类型。
5. 企业身份权限、备份、监控和部署。

### 当前暂缓

- 继续扩展未经验证的物理算法；
- 正式参数库校准；
- 外部软件盲算；
- 生产统一认证；
- 电子签章；
- 正式客户报告；
- 复杂微服务拆分。

---

## 16. 风险登记册

| 风险 | 影响 | 应对 |
|---|---|---|
| 合成 JSON 字段无法自然出现在客户资料中 | 为了凑齐字段而伪造不合理文件 | 把模型参数移入系统参数包，只在资料中放项目事实 |
| 在线大模型输出波动 | 自动化验收不稳定 | 确定性回放作为强制验收，在线模型单独测试 |
| 合成资料过于理想 | 无法验证异常处理 | 建立 D10 至 D70 条件变体 |
| 大模型在报告中改写数字 | 报告与结果不一致 | 使用指标引用和确定性数字替换 |
| 用户误认为报告可正式使用 | 法律和安全风险 | 水印、用途声明和发布门禁 |
| 文件生成二进制哈希漂移 | 回归不稳定 | 校验业务内容哈希和标准化哈希 |
| 第一部分字段覆盖不完整 | 无法达到11/11 | 阶段1先完成逐字段矩阵 |
| 继续无序扩展功能 | 主链长期不闭合 | 所有需求先判断是否阻断 M1.5 |
| 缺少 QRA 专家 | 正式模型无法批准 | 演示版与正式版分离，不伪造批准 |
| 模型服务不可用 | 演示中断 | 提供离线确定性演示模式 |

---

## 17. 项目管理和记录方式

### 17.1 每阶段完成记录

每个阶段完成后记录：

- 阶段状态；
- 开始和完成日期；
- 实施人；
- 对应提交或版本；
- 变更文件；
- 测试命令；
- 测试结果；
- 输入哈希；
- 快照哈希；
- 数值结果哈希；
- 未解决问题；
- 是否允许进入下一阶段。

### 17.2 状态更新原则

- 代码存在不等于完成；
- 文档存在不等于验收；
- 单元测试通过不等于端到端通过；
- 合成数据通过不等于真实数据通过；
- 计算成功不等于正式报告可签发；
- 只有满足本阶段全部验收条件后才能标记 PASS。

### 17.3 建议每周复盘

每周只回答五个问题：

1. 当前原始资料能覆盖多少关键字段？
2. 当前可以运行多少个节点？
3. 当前最主要阻断项是什么？
4. 本周新增能力是否让完整闭环更近？
5. 是否出现把合成结果误写成真实结论的风险？

---

## 18. 完成定义

本项目只有同时满足以下条件才算完成：

1. 用户从页面创建或加载合成项目。
2. 系统接收多格式合成原始资料。
3. 系统执行真实的解析、OCR、提取、标准化和冲突处理流程。
4. 用户无需接触代码和 JSON。
5. 所有关键项目事实都有原始证据。
6. 所有系统参数都有版本化参数包来源。
7. 所有场景假设都有明确记录。
8. 服务端门禁通过后形成不可变快照。
9. 快照与黄金答案一致。
10. 自动完成 11/11 计算节点。
11. 结果与现有合成基线一致。
12. 自动生成图表和受控报告草稿。
13. 报告所有数字与计算结果一致。
14. 报告明确标记全部数据为人工合成。
15. 正式报告门禁保持关闭。
16. 正向和负向端到端测试全部通过。
17. 新环境可以按手册重复启动和演示。
18. 全流程可以从报告反查到结果、快照、复核决定和原始资料。

最终状态：

> `M1_5_FULL_SYNTHETIC_END_TO_END_ACCEPTED`

达到该状态后，项目即可对外展示“软件全流程已经跑通”。后续真实数据到位时，应逐项替换第一层项目事实和第二层参数库，并重新执行数据验收、模型验证和正式发布审批，而不需要重新设计整套软件架构。

---

## 19. 第一批立即执行任务

建议从以下顺序开始，不并行扩散范围：

- [x] 任务1：建立 `field-source-node-matrix.csv`。
- [x] 任务2：确认 S00 标准案例中哪些属于项目事实、模型参数和场景假设。
- [x] 任务3：实现 `build_synthetic_source_pack.py` 的 XLSX/CSV 输出。
- [x] 任务4：生成 S00×D00 第一版合成原始资料包。
- [x] 任务5：用现有上传页面导入该资料包，记录缺失候选和映射差异。
- [x] 任务6：逐项补齐映射和提取合同（2026-09-02 离线验收：361/361 均有实现路径；不等同于 361 项都由文件直接提取）。
- [x] 任务7：生成与黄金答案一致的不可变快照。
- [x] 任务8：从该快照运行 11/11。
- [x] 任务9：把这条路径固化为集成测试。
- [x] 任务10：在主链通过后再开始统一向导和报告智能体。

2026-09-02 最终离线复验：阶段 1–7 全部通过，最终端到端门为 19/19，完整回归为 `312 passed, 1 skipped`；跳过项是未启用的在线 OCR 条件测试，本轮未进行任何新的外部调用。
