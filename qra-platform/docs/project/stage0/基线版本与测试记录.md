# 基线版本与测试记录

## 1. 代码基线

| 项目 | 冻结值 |
|---|---|
| 平台包版本 | `pipeline-qra-platform 0.8.0` |
| Git 分支 | `main` |
| 基线提交 | `66bd790a19da3432df85ce18ce64be5877137e2a` |
| Git tree | `efa5bd7779b24e4adb27910583a1a2ee28def3df` |
| 基线标签 | `stage0-code-baseline-v0.8.0` |
| Python | `3.10.11` |
| 依赖检查 | `No broken requirements found` |
| 冻结日期 | 2026-08-26 |

标签为只读基线标识。后续变更以新提交、新版本或新标签体现，不得移动或覆盖该标签。

## 2. 基线规模

- Python 源文件：56；
- 自动化测试文件：23；
- 模型规格 JSON：8；
- 通用映射 JSON：2；
- 根基线提交：140 个受控文件，23,312 行新增记录。

真实项目源资料、SQLite、运行输出、临时文件和许可软件均受 `.gitignore` 隔离，不属于代码基线。

## 3. 测试证据

执行日期：2026-08-26。

```text
ARCHITECTURE CHECK PASSED
Ran 103 tests in 36.255s
OK
```

测试命令：

```powershell
.\.venv\Scripts\python.exe .\tools\check_architecture.py
.\.venv\Scripts\python.exe -m unittest discover -s tests -t . -v
```

结论：架构检查通过，103/103 项自动化测试通过。该证据只说明软件回归基线通过，不等于真实资料、生产参数或工程模型已验收。

## 4. 模型注册状态

| 状态 | 数量 | 说明 |
|---|---:|---|
| `RELEASED` | 2 | 工程指标数据结构、GB/T 34346 附录 C 个人风险参考准则 |
| `IMPLEMENTED_*` | 2 | 标准公式已实现，但仍有生产参数、基准或收敛性阻断项 |
| `PROVISIONAL_*` | 1 | 证据条件化筛查模型，只可用于受控筛查 |
| `SYNTHETIC_TEST_ONLY` | 3 | 只用于测试，不得用于正式工程结论 |

完整规格与映射哈希见 [baseline-files.sha256](baseline-files.sha256)。

## 5. 候选试点预检记录

| 项目 | 九江 | 芦溪 |
|---|---:|---:|
| 正式输入预检 | `BLOCK` | `BLOCK` |
| 动态执行状态 | `PARTIAL` | `PARTIAL` |
| 完成/总节点 | 6/11 | 6/11 |
| 缺失输入项 | 13 | 13 |
| 正式风险接受性判断 | 禁止 | 禁止 |

阶段 0 比较运行产物存放在本机 `workspace/runtime/stage0-pilot-evaluation/`，属于可再生运行证据，不纳入 Git。
