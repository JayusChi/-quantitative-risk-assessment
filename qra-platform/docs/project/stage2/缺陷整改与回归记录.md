# 阶段 2 缺陷整改与回归记录

## 1. 软件缺陷

| 编号 | 等级 | 试算暴露问题 | 整改 | 状态 |
|---|---|---|---|---|
| DEF-M2-001 | P1 | 0 个空间人口单元时，筛查结果把最大 IR 写为 `0.0` 并生成 F-N 图，可能把“不可算”误解为“低风险” | IR 改为 `null` + `NOT_CALCULATED_MISSING_SPATIAL_RECEPTORS`；F-N 标记不可得并不生成 IR/F-N/融合图；HTML 显示“不可算（缺空间受体）” | `CLOSED_WITH_REGRESSION` |
| DEF-M2-002 | P1 | `human_qra` 被上游依赖阻断时未运行自身预检，`missing_inputs` 为空，遗漏气象、人口和点火缺口 | 规划阶段即使依赖阻断也执行安全预检，完整展开复合节点输入合同 | `CLOSED_WITH_REGRESSION` |

本次开放 P0/P1 软件缺陷为 0。数据、参数、模型验证和业务复核缺口仍开放，不能用“软件缺陷已关闭”替代它们。

## 2. 回归覆盖

新增 `tests/integration/test_stage2_jiujiang_trial.py`，使用阶段 1 脱敏黄金业务 JSON 验证：

1. 两次运行数值哈希一致；
2. 5 个预期节点完成、6 个预期节点跳过、0 个节点失败；
3. 完整 QRA 节点列出 assessment、频率、气象、人口和点火缺口；
4. 筛查 PLL 仍可生成；
5. 缺空间受体时 IR 为 `null` 而不是 0；
6. 缺人口分布时不输出项目 F-N；
7. HTML 报告显示 IR 不可算且不生成相应误导性图表。

`qra-engine` 从 0.6.0 更新为 0.6.1，动态输出合同从 1.1.0 更新为 1.2.0，平台包更新为 0.9.1。

## 3. 机器证据

本机受控目录 `workspace/runtime/stage2-real-data-trial-final/` 保存：

- `execution-difference-register.json`：四层排查和缺陷闭环；
- `run-a-artifacts/`、`run-b-artifacts/`：两次完整节点输出；
- `stage2-web.sqlite3`：从阶段 1 数据库副本执行后的计算、节点、结果和审计；
- `stage2-run-a-export.zip`：通过真实管理 API 导出的结果包；
- `stage2-acceptance-summary.json`：验收总表。
