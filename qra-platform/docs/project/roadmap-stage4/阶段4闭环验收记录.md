# 路线图第四阶段闭环验收记录

## 结论

本次验收状态为：

```text
M1_INTERNAL_PILOT_PASS / FORMAL_BUSINESS_SIGNOFF_PENDING
```

批准的真实受控原始资料已从平台文件入口完成解析、候选与证据生成、虚拟内部角色逐项复核、服务端门禁、不可变快照、自动计算、筛查报告和反向追溯。该结论仅证明内部功能试点闭环，不等于正式业务签批、完整 11/11 QRA、风险接受性结论或监管用途批准。

## 本次事实链

| 项目 | 结果 |
|---|---|
| 验收模式 | `pilot` |
| 原始文件数 | 6 |
| 来源清单 SHA-256 | `8fafffe9801e850d766c1c20928885d46cba79d686f4cf0fca68e68f876ec812` |
| 转换任务 | `CONV-20260831T074458Z-fd2d0320` / `CONFIRMED` |
| 解析产物 | 303 |
| 提取实体 | 1 |
| 候选字段 / 证据链接 | 5 / 5 |
| 复核会话 | `RSESS-2acf8d67-0025-4e33-8ed3-1ff294e02ddb` / `CONFIRMED` |
| 明确复核决定 | 5 |
| 服务端门禁 | `PASS`，阻断 0、警告 0、可运行目标 5 |
| 不可变快照 | `SNAP-76dfc18b-9350-45e2-811e-08b8941a5033` |
| 快照 payload SHA-256 | `fa52f9c1a5813ae5fdc91d6d598f7954664295c3c85f6cc315a65bb662715624` |
| 计算任务 | `RUN-20260831T082059Z-4297033e` / `COMPLETED` |
| 管段结果 | 1 |
| 反向追溯 | `PASS` |
| 确定性复跑 | `PASS` |
| 真实浏览器验收 | `PASS` |

五个版本化目标节点全部完成，目标节点无失败或跳过：

1. `data_inventory`
2. `indicator_coverage`
3. `segment_geometry`
4. `adaptive_evidence_qra`
5. `risk_matrix`

报告资源共 19 项，数据库登记的相对路径、字节数和 SHA-256 均通过完整性检查。HTML 报告明确标注筛查边界；缺失的昼夜人口显示为“未记录”，不可计算指标显示为“不可算”，未把缺失值静默写成 `0`。

## 浏览器业务验收

使用普通用户可见的本地页面完成了以下操作：

- 选择版本化九江映射并上传 6 个受控原始文件；页面没有粘贴 `review_decisions JSON` 的业务入口。
- 查看转换状态、逐文件解析、候选、证据和问题统计。
- 在三栏工作台查看表格单元格定位证据，并打开同一转换任务的 PDF 解析预览。
- 以 `stage4-internal-functional-reviewer` 身份逐项保存 5 条明确决定，revision 从 1 递增到 6。
- 服务端门禁通过后填写快照名称、确认人和理由，勾选“确认后立即创建计算任务”和“生成图表”。
- 页面显示本次 `snapshot_id`、`run_id`、`COMPLETED` 和 M1 `PASS`，并从结果对话框打开本次筛查报告。
- 检查 1280×900 桌面宽度和 720×900 窄屏；窄屏无关键内容遮挡，Tab 可到达可见链接且有焦点指示。
- 最终报告页控制台错误 0、警告 0。

含资料正文或客户标识的截图只保存在受控 `workspace/runtime/roadmap-stage4/`，不进入版本控制。本记录只保留哈希：

- PDF 解析预览：`ef8c97ec03b52697e1d155912cf231d986bd0628c3bfb3734597d340c65e6505`
- 720px 筛查报告：`ec323222acbf0893f906a20c1a7b0a63ba41de7561237eccabb7107d3f0ef385`
- 1280px 筛查报告：`a37fdf3823dcd0da98ea07c67613a2985dd2613b3bfc9c96eb285ee944b9a110`
- 最终闭环结果对话框：`c1460620850f38b7cd01afcd9ec7cd304d3cde518eb1dce3e1a33b7d204340e8`

## 真实断点与修复

首次浏览器上传采用完全通用化的文件别名，暴露出现有映射依赖语义文件名模式，导致解析成功但没有形成候选。该失败任务和解析事实被保留。随后仅在受控运行目录使用保留必要语义、仍不暴露真实文件名的别名，成功命中版本化映射。

首份浏览器报告又暴露出缺失昼夜人口被呈现为 `0 / 0`、待补数据标签容易被理解为完整 QRA 已无缺口的问题。报告模板已修正；历史报告保持不可变，并针对同一不可变快照创建新计算任务验证修复。最终验收只引用上表中的本次运行。

## 外部模型与数据边界

- 试点 manifest 的 `external_sharing_allowed` 为 `false`。
- 本次真实公网 OCR/大模型外发调用：0 次。
- 外发失败调用：0 次。
- 未使用 API Key、Authorization 或外部模型完整响应。
- 原始资料、解析正文、候选实际值和证据正文未写入 Git 跟踪验收记录。

## 自动化验证

已执行的专项结果：

- `.\.venv\Scripts\python.exe -m compileall -q src tests tools`：退出码 0。
- `.\.venv\Scripts\python.exe tools\check_architecture.py`：`ARCHITECTURE CHECK PASSED`。
- `.\.venv\Scripts\python.exe tools\run_roadmap_stage3_acceptance.py --json`：22 tests，16.059 秒，`OK`；请求模式通过，真实 OCR 未请求。
- `.\.venv\Scripts\python.exe tools\run_roadmap_stage4_acceptance.py --mode engineering --json`：退出码 0，`ENGINEERING_PASS`；工程模式浏览器状态按定义为 `PENDING`，不冒充真实试点。
- `python -m unittest tests.integration.test_roadmap_stage4_e2e -v`：9 tests，22.082 秒，`OK`。
- `python -m unittest tests.integration.test_review_workbench -v`：16 tests，32.346 秒，`OK`。
- 真实试点一键验收：退出码 0，`M1_INTERNAL_PILOT_PASS`。
- 浏览器证据与同一 conversion/review/run 三个 ID 一致；确定性复跑 `PASS`。
- `.\scripts\test.ps1`：架构检查通过；254 tests，127.022 秒，`OK (skipped=1)`。唯一跳过项为未配置且未授权的真实 OCR smoke test。

## 未完成的正式批准

以下事项仍明确保留，不因本次内部试点 PASS 自动解除：

- `FORMAL_BUSINESS_SIGNOFF`
- `COMPLETE_11_OF_11_QRA_DATA_AND_EXPERT_APPROVAL`

机器可读记录：[stage4-e2e-acceptance.json](./stage4-e2e-acceptance.json)。
