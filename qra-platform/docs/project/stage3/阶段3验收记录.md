# 第三阶段验收记录

执行时间：2026-08-28 10:28（Asia/Shanghai）

## 验收结论

第三阶段正式验收通过，可以关闭并向第四阶段移交。

- 确定性专项验收：`PASS`；共运行 35 项，失败 0，错误 0，跳过 1 项；
- 九江真实 OCR：`PASS`；
- 正式扫描件验收：`PASS`；
- 仓库全量回归：182 项通过、跳过 1 项；
- 架构检查：`PASS`。

专项套件中的 1 项跳过是旧的环境变量型真实 OCR 冒烟用例。正式验收已经通过
`real-ocr-acceptance.json` 对九江真实输入独立执行，因此该跳过不构成阶段阻断。

## 执行命令

确定性专项验收：

```powershell
python .\tools\run_stage3_acceptance.py --json
```

正式真实 OCR 验收：

```powershell
python .\tools\run_stage3_acceptance.py `
  --require-real-ocr `
  --json `
  --record .\docs\project\stage3\real-ocr-acceptance.json
```

全量回归：

```powershell
powershell -NoProfile -ExecutionPolicy Bypass -File .\scripts\test.ps1
```

## 九江真实 OCR 结果

提供方为 `aliyun-bailian-dashscope`，模型为 `qwen3.5-ocr`，配置来自 Windows
当前用户 DPAPI 加密存储。

### 真实 JPG

- 文件：`001.jpg`；
- 输入 SHA-256：`d53f3385ba6ed037e8889cb728efe02cba1493d642df12d7cdd05522eaa42bc4`；
- 页数：1；
- OCR 块：3；
- 带页码、坐标和坐标系的 OCR 块：3，证据绑定率 100%；
- 原始响应 SHA-256：`b2682aa016b0eb32427e5ef3c48db2b55c2d0b46d025489fda8811644c593763`；
- 结果：`PASS`。

### 两页扫描 PDF

- 文件：`jiujiang-real-scan-acceptance.pdf`；
- 输入 SHA-256：`8697e29759fb93264e5b0df926ee20382fe429338addd494936dfed533827611`；
- 页数：2，两页均分类为 `SCAN`；
- OCR 块：7；
- 带页码、坐标和坐标系的 OCR 块：7，证据绑定率 100%；
- 第 1 页原始响应 SHA-256：
  `0ffdf12dead7a8b1885755523e733fe0a37a254e47b33b87764b37ee44d1a52a`；
- 第 2 页原始响应 SHA-256：
  `3103ced1e59cb3aa69bfe2496a0f5651f31a03bf86cd4bcc677fae75160b1f3f`；
- 结果：`PASS`。

图片和 PDF 均正确标记了倾斜、内容触边等质量警告；警告没有被当成错误，也没有阻断
坐标证据固化。

## 审计与版本溯源

`parsed_document.json` 和 `preview_manifest.json` 现统一保存：

- 源文件 SHA-256；
- 读取器 ID 和版本；
- OCR 提供方 ID 和模型版本；
- OCR 语言、表格检测和超时参数；
- 图像预处理版本；
- 解析合同版本；
- 缓存键；
- 外部 OCR 请求 ID 和原始响应 SHA-256。

这些字段进入规范化解析哈希或其绑定的来源链。验收 JSON 不包含 API Key、端点凭据或
OCR 识别正文。

## 黄金夹具补充

本次新增或补齐：

- 真实 BIFF `.xls`：多工作表、公式及缓存值、合并单元格、日期、空值和前导零；
- 同一 PDF 内的原生页与扫描页路由；
- 旋转扫描 PDF 的角度与坐标边界；
- 损坏 PDF、空 CSV、超限图片的结构化错误；
- 模糊、过曝、欠曝和低分辨率图片质量检测；
- OCR 网络拒绝、超时和服务端错误的准确映射；
- 版本溯源在缓存命中和预览清单中的一致性。

## 安全检查

- API Key 只存在于本机 DPAPI 加密配置和验收进程内存；
- `workspace/state/ocr-settings.json` 保持 Git 忽略；
- 管理接口、日志、测试输出和验收记录均不返回 API Key；
- 正式验收记录只保存输入哈希、版本、请求 ID、响应哈希、计数和问题码；
- 临时受限 TLS 隧道在验收完成后已停止。

## 阶段出口

第三阶段要求的统一解析合同、证据位置、质量问题、版本化缓存、真实 OCR 和全量回归均已通过。
第三阶段状态更新为 `CLOSED/PASS`，第四阶段可以开始消费 `ParsedDocument`、质量报告、预览资源
和证据 ID；不得覆盖解析原文、坐标或低置信度标记。
