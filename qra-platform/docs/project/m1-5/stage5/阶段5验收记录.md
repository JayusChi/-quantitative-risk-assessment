# 阶段 5 验收记录

验收日期：2026-09-01  
场景：`S00_BASELINE × D00_CLEAN`  
阶段门：`S5_USER_JOURNEY_ACCEPTED`  
结果：`PASS`（11 / 11）

## 阶段切片

| 检查 | 结果 | 验证内容 |
|---|---|---|
| S5-01 | PASS | 项目级聚合、普通项目和演示项目状态 |
| S5-02 | PASS | 10 份合成资料、6 个参数包、演示加载幂等性 |
| S5-03 | PASS | 上传到报告的六步项目向导 |
| S5-04 | PASS | 统一业务状态与 11 / 11 节点进度 |
| S5-05 | PASS | 普通入口无 JSON 粘贴和数据库操作 |
| S5-06 | PASS | 高级审计和全链路标识/哈希 |
| S5-07 | PASS | 合成标识、草稿状态、正式报告门关闭 |
| S5-08 | PASS | 失败重试、继续处理、已确认快照保留 |
| S5-09 | PASS | 1440px 桌面与 390px 窄屏响应式布局 |
| S5-10 | PASS | 键盘焦点、跳转链接、错误提示、复核返回项目 |
| 端到端导航 | PASS | 报告、计算、输入资料均留在项目上下文内 |

## 演示项目结果

| 指标 | 结果 |
|---|---:|
| 原始资料 | 10 份 |
| 参数包 | 6 / 6 |
| 计算节点 | 11 / 11 完成 |
| 数据完整性 | PASS |
| 数字一致性 | PASS |
| 引用绑定 | BOUND |
| 报告产物 | HTML + ZIP（17 个运行产物） |
| 正式报告判定 | false |

项目聚合状态为 `REPORT_READY`；六步向导前五步完成、报告步骤激活。数据快照、结果哈希、引擎版本和参数包均可从高级审计区反查。

## 真实浏览器验收

- 桌面：1440 × 900，检查项目列表、演示项目、六步向导、10 份资料、11 节点、报告中心和页内 HTML 报告。
- 窄屏：390 × 844，页面 `scrollWidth` 等于视口宽度 390，无横向溢出；项目详情和复核页面返回项目入口可见。
- 键盘：Tab 可聚焦跳转链接，Enter 跳到主内容并可打开新建项目对话框，Escape 可关闭对话框。
- 错误：空项目名显示“请填写项目名称”，空文件选择显示“请至少选择一份项目资料”，均通过 `role=alert` 暴露。
- 有效演示项目页面控制台错误 0、警告 0；报告 iframe 保持项目导航并加载真实动态报告。

浏览器截图保存在 `output/playwright/`：

- `stage5-projects-desktop.png`
- `stage5-keyboard-skip-focus.png`
- `stage5-demo-detail-desktop.png`
- `stage5-demo-detail-mobile.png`
- `stage5-review-mobile.png`

## 边界说明

- 阶段 5 提供 HTML 与 ZIP 的可用导出入口；PDF/DOCX 入口明确标注为阶段 6 才开放，与路线图“根据交付需要补充 PDF 或 DOCX”一致。
- 合成数据仅用于内部演示和验收，不能用于实际生产结论、政府申报或安全决策。
- 阶段门记录是可重复生成的机器验收，不替代未来真实资料回归和报告人工审批。

## 自动化验证

- 阶段 5 专项测试：6 项通过。
- 入口和数据库版本兼容回归：14 项通过。
- 阶段 3 依赖分层回归：11 项通过。
- 全量回归：292 项通过，1 项按既有条件跳过；无失败。
- 阶段 5 及依赖分层改动 Ruff：通过。
- Python 编译检查、仓库架构检查和 `git diff --check`：通过。

命令与结果见 `test-results.txt`。

