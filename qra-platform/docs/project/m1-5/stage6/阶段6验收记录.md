# 阶段 6 验收记录

验收日期：2026-09-01  
场景：`S00_BASELINE × D00_CLEAN`  
阶段门：`S6_CONTROLLED_REPORT_AGENT_ACCEPTED`  
结果：`PASS`（14 / 14）

## 阶段切片

| 检查 | 结果 | 验证内容 |
|---|---|---|
| S6-01 | PASS | `report-context-v1`、40 个指标和 11 个节点 |
| S6-02 | PASS | `report-draft-v1`、固定 15 章和提示版本 |
| S6-03 | PASS | 不可变快照与结果到报告上下文的确定性转换 |
| S6-04 | PASS | 结构化模型输出、提供器标识和不同文字草稿 |
| S6-05 | PASS | 数字引用和渲染后数字一致性 |
| S6-06 | PASS | 11 个证据项和 11 个结果项引用 |
| S6-07 | PASS | 节点状态一致性正例与缺失节点负例 |
| S6-08 | PASS | 禁用表述和无来源项目事实负例 |
| S6-09 | PASS | HTML、PDF、DOCX 合成数据水印 |
| S6-10 | PASS | 大模型不可用时确定性模板降级 |
| S6-11 | PASS | 人工确认到 `CONFIRMED_TEST_ONLY`，正式门保持关闭 |
| S6-12 | PASS | 两个版本、上下文/草稿哈希和数据库不可变触发器 |
| S6-13 | PASS | HTML 和包含 10 个文件的追溯 ZIP |
| S6-14 | PASS | PDF 与 DOCX 输出、文件哈希和结构可读性 |

## 关键结果

| 指标 | 结果 |
|---|---:|
| 报告章节 | 15 |
| 受控指标 | 40 |
| 证据索引 | 11 |
| 结果索引 | 11 |
| 计算节点 | 11 / 11 完成 |
| 参数绑定 | 6 |
| 版本数 | 2 |
| 人工确认状态 | `CONFIRMED_TEST_ONLY` |
| 正式报告判定 | `false` |

最终生成 HTML、PDF、DOCX、ZIP、上下文 JSON、草稿 JSON 和校验 JSON。大模型结构化提供器与确定性降级模板在同一上下文下保持数字、图表和引用目标不变；自由文本变化不会进入数值计算路径。

## 负例和边界验证

- 向自由文本注入数字会被数字引用检查拒绝。
- 把缺失或跳过节点写成已完成会被节点一致性检查拒绝。
- “正式评价通过”、自行认定风险可接受、解除发布阻断等表述会被拒绝。
- 未绑定到结果或证据的项目事实会被拒绝。
- 模型提供器抛出异常时仍能输出完整模板报告，并记录降级原因和原请求提供器。
- 人工确认不等于生产审批，不会改变 `formal_report_allowed=false`。

## 文档和 PDF 质量检查

- PDF：Letter 纸、1 英寸页边距，共 7 页；最终版逐页目检，封面、水印、表格、图表、页眉页脚和分页正常。
- DOCX：无障碍审计高/中/低问题均为 0；3 张图片均有替代文本；6 张表的 `tblW`、`tblInd`、`tblGrid` 和全部 `tcW` 精确一致；标题层级、页边距、页眉页脚、水印和图片嵌入结构通过。
- 当前验收主机未安装 LibreOffice，因此 DOCX 未执行 DOCX→PNG 的直接视觉渲染；以上 DOCX 结论来自 OOXML 结构审计。PDF 已完成实际渲染和逐页视觉检查。

## 自动化验证

- 阶段门：14 / 14 通过。
- 阶段 6 专项：4 项通过。
- 阶段 2 兼容性与阶段 6 组合回归：10 项通过。
- 全量回归：296 项通过，1 项按既有条件跳过；无失败。
- Ruff、Python 编译、仓库架构检查和 `git diff --check`：通过。

详细命令与结果见 `test-results.txt`，机器记录见 `resources/synthetic/full-chain-v1/stage6/stage6-acceptance.json`。

