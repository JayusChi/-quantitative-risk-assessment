# M1.5 基线版本记录

## 1. Git 基线

| 项目 | 冻结值 |
|---|---|
| 仓库根目录 | `D:\风险定量评估` |
| 项目目录 | `D:\风险定量评估\qra-platform` |
| 分支 | `codex/stage0-takeover-20260828` |
| 基线提交 | `1ebdc65c6169f6c5a999c80bfa9b7c74f3b1a99f` |
| Git tree | `e8bf0ea9759ff36b264b21822157e397afc3da56` |
| 回退标签 | `roadmap-stage4-internal-pilot-20260901` |
| 上游状态 | 相对 `origin/codex/stage0-takeover-20260828` ahead 1 |
| 平台包版本 | `pipeline-qra-platform 0.9.1` |
| 数据库 Schema | `2.1.0` |
| 动态结果 Schema | `1.3.0` |
| Python | `3.10.11` |
| 冻结日期 | 2026-09-01 |

基线提交的最后一条提交说明为 `feat: complete OCR robustness and internal pilot loop`。当前标签可作为只读回退点；本阶段未移动标签、创建提交或修改历史。

## 2. 工作树状态

采集基线时不存在已跟踪文件修改，也不存在暂存区修改。仓库根目录只有用户提供的未跟踪需求文档：

```text
?? 合成数据跑通项目.md
```

该文件的 SHA-256 为：

```text
cfd9441dc8d7e572892c193b8913320b7fc8f3f39460949ed2b94af313d075ce
```

它是本次 M1.5 需求来源，但不属于冻结的 Git tree。本目录已经把阶段 0 所需的范围、完成定义、决策和风险口径写入受控交付物，后续若需求文档内容变化，必须按决策变更规则重新评估。

## 3. 基线规模

- 项目目录内已跟踪文件：377；
- Python 源文件：128；
- 自动化测试文件：49；
- 引擎模型规格 JSON：8。

## 4. 依赖基线

Python 直接运行依赖的实测版本为：

| 依赖 | 版本 |
|---|---:|
| jsonschema | 4.26.0 |
| openpyxl | 3.1.5 |
| Pillow | 12.3.0 |
| pdfplumber | 0.11.10 |
| pypdf | 6.16.2 |
| xlrd | 2.0.2 |

完整环境清单见 [dependencies.txt](dependencies.txt)。`pip check` 结果为 `No broken requirements found.`。

## 5. 架构基线

执行命令：

```powershell
$env:PYTHONPATH = (Join-Path (Get-Location) 'src')
.\.venv\Scripts\python.exe .\tools\check_architecture.py
```

结果：`ARCHITECTURE CHECK PASSED`。

## 6. 新环境启动冒烟

在 `workspace/runtime/m1-5-stage0-smoke-env-20260901/` 新建虚拟环境，使用当前机器已安装的依赖完成以下检查：

1. 导入 `qra_engine`、`qra_converter`、`db_qra` 及全部直接依赖；
2. 读取动态节点目录，确认节点数为 11；
3. 初始化新的 SQLite 数据库，确认 Schema 为 `2.1.0`；
4. 启动 `qra_engine`、`qra_converter` 和 `db_qra` 的命令行入口。

结果：`NEW_ENVIRONMENT_STARTUP_SMOKE=PASS`。

该冒烟证明当前代码可在新虚拟环境中启动。完整的跨机器离线重建仍依赖后续维护可安装依赖源或锁定制品，已在风险登记册中持续跟踪。

## 7. 核心基线文件哈希

核心输入、检查器、生成器、数据库实现和动态引擎的 SHA-256 见 [baseline-input-files.sha256](baseline-input-files.sha256)。

