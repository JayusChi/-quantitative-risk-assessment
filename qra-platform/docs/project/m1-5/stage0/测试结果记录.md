# M1.5 阶段 0 测试结果记录

## 1. 执行结果

执行日期：2026-09-01。

```text
ARCHITECTURE CHECK PASSED
Ran 254 tests in 114.947s
OK (skipped=1)
```

计数口径：共执行 254 项测试，其中 253 项通过、1 项按条件跳过；failure 0、error 0。因此“现有 254 项测试无回归失败”的阶段门满足。跳过项为：

```text
tests.integration.test_document_parsing_pipeline.
DocumentParsingPipelineIntegrationTest.test_real_ocr_provider_smoke
```

该项只在配置真实在线 OCR 服务和正式扫描样本时运行。阶段 0 的确定性回归不得依赖公网服务，因此此条件跳过不构成失败。

## 2. 执行命令

```powershell
$env:PYTHONPATH = (Join-Path (Get-Location) 'src')
.\.venv\Scripts\python.exe .\tools\check_architecture.py
.\.venv\Scripts\python.exe -m unittest discover -s tests -t . -v
```

工作目录：`D:\风险定量评估\qra-platform`。

## 3. 覆盖边界

本次回归覆盖现有单元测试和集成测试，包括资料读取、文件安全、OCR 适配、字段提取、标准化、融合、复核、快照、数据库、动态计算、11 节点规划、报告和反向追溯。

该结果证明当前软件基线无已知自动化回归失败，不证明在线模型稳定性、真实工程数据完整性、生产参数批准或正式 QRA 可签发。

简版原始记录见 [test-results.txt](test-results.txt)。

