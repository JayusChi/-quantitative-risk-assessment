# 在线大模型真实验收记录

验收日期：2026-09-02  
范围：`S00_BASELINE × D00_CLEAN`、`S00_BASELINE × D10_CONFLICT`  
提供方：阿里云百炼  
模型：`qwen3.8-max`  
数据分类：仅全合成测试资料  
结论：`PASS`

## 授权与安全边界

- 账号持有人已明确授权本次将全合成测试资料发送到已配置的阿里云百炼工作空间，并知悉可能产生调用费用。
- API Key 由 Windows 当前用户 DPAPI 加密保存；命令、验收结果和本文均不包含密钥。
- 模型无工具权限，只能返回严格 JSON Schema；字段路径和证据 ID 使用本地白名单校验。
- 在线结果必须进入人工复核，不能直接生成或持久化不可变计算快照。

## 验收结果

| 场景 | 批次 | 正确 | 精确率 | 召回率 | 证据绑定率 | 重试 | Token | 结果 |
|---|---:|---:|---:|---:|---:|---:|---:|---|
| D00_CLEAN | 5 / 5 | 69 / 69 | 100% | 100% | 100% | 0 | 32,553 | 通过，待人工复核 |
| D10_CONFLICT + BASE | 5 / 5 | 69 / 69 | 100% | 100% | 100% | 0 | 32,723 | 通过，待人工复核 |

D10 的 `pipeline.operating_pressure_mpa` 出现 `8.35 MPa` 与 `8.0 MPa` 冲突。验收者明确选择 `BASE=8.0 MPa`，复核项状态为 `HUMAN_CONFIRMED`，随后在线模型仅验证人工选择后的 69 项候选。

在线工作流门禁保持 `BLOCKED` 是预期设计：每次在线运行都会增加 `ONLINE_MODEL_DEMO_PENDING` 人工复核项，因此 `COMPLETED_REVIEW_REQUIRED` 表示在线能力验收成功，而不是授权模型绕过复核生成快照。

## 超时观察与整改

首次把 69 项放入单个 64,964 字节请求时，连续三次在 120 秒超时，安全回退状态为 `MODEL_CALL_FAILED_PRESERVED_AND_ROUTED_TO_HUMAN`；解析产物保留、无快照写入。随后用单字段连通性请求确认凭据、工作空间和模型路由正常，并将 69 项改为每批最多 15 项。最终 D00 与 D10 共 10 个批次全部成功，单批耗时约 24～73 秒，无重试。

这次整改只改变在线请求的分批方式，不改变字段合同、标准化规则、证据绑定、人工决策或确定性主链。

## 验收门槛判定

| 门槛 | 要求 | 实测 | 判定 |
|---|---:|---:|---|
| 关键字段精确率 | ≥ 95% | 100% | PASS |
| 关键字段召回率 | ≥ 90% | 100% | PASS |
| 最终证据绑定率 | 100% | 100% | PASS |
| 冲突人工确认 | 全部 | 1 / 1 | PASS |
| 失败安全回退 | 必须保留解析产物与人工入口 | 已验证 | PASS |

## 脱敏证据

- D00 结果：`workspace/outputs/m1-5-stage3-online-real-chunked-20260902/D00_CLEAN/stage3-result.json`，SHA-256 `a73a1b224b236f2b6c7daee104f226ec6a26dd14622b64a1ec110f623b07409c`。
- D10 结果：`workspace/outputs/m1-5-stage3-online-real-chunked-20260902/D10_CONFLICT_BASE_CONFIRMED/stage3-result.json`，SHA-256 `0b8d508a2ca35bc40478d8c4d6e5f19846a05b5a5d8a4c471f3d738202b1cf4f`。
- 首次超时结果：`workspace/outputs/m1-5-stage3-online-real-20260902/D00_CLEAN/stage3-result.json`，SHA-256 `5b8162c6f84d4372870d05f827113967cb5c9c4a1dbcab7ed220172f83ec1450`。
- 机器判定：`resources/synthetic/full-chain-v1/stage3/real-online-acceptance.json`。

证据文件仅保存结构化指标、提供方请求 ID、请求/响应哈希、Token 用量、耗时和脱敏错误码，不保存 API Key 或模型原始响应正文。

## 回归验证

- 阶段 3 专项测试：13 / 13 通过。
- 全量自动化测试：311 项通过、1 项按既有条件跳过、0 项失败。
- 本次改动文件 Ruff：通过。
- 项目架构检查：`ARCHITECTURE CHECK PASSED`。
- 全仓库 Ruff 会同时扫描历史源码和 `workspace/releases` 发布副本，仍有 943 个既有格式问题；这些问题不由本次改动引入，且未掩盖本次文件的定向检查结果。
