# 阶段 1 真实数据转换验收记录

## 1. 执行范围

本记录只验收真实资料盘点、专用映射、转换、复核、黄金候选、命令行/网页一致性、快照和回归门禁。它不执行阶段 2 风险试算，不批准生产参数，不发布模型，也不签发正式工程报告。

## 2. 验收标准核对

| 验收标准 | 技术结果 | 证据 |
|---|---|---|
| 管段编号唯一 | 通过 | 仅 `GDBZYQ-JJ-1` 1 段 |
| `start_km < end_km` 且长度一致 | 通过 | 0.000 < 10.938；差值和长度均 10.938 km |
| 无未解释重叠、断点、越界 | 通过 | 单段覆盖冻结边界；质量校验 0 error |
| 关键记录唯一关联或转人工 | 通过 | 台账第 4 行唯一；DOCX 无位置内容转人工并确认不导入 |
| 单位来源与换算有记录 | 通过 | 42 条 lineage；10938 m → 10.938 km；273mm 显式解析 |
| 空值不自动变 0 | 通过 | 无人口单元；温度、壁厚、空间人口显式缺失 |
| 关键值可追溯 | 通过 | 全部关键业务值回溯到文件哈希、Sheet1、row 4 和列名 |
| 冲突/低置信度审计完整 | 通过（技术） | 1 个辅助内容决定；含复核人、时区时间、原因和前后值 |
| 命令行/网页 JSON 深度一致 | 通过 | 两端哈希均 `0da428...e7d4` |
| 重复转换哈希一致 | 通过 | 两次命令行哈希均 `0da428...e7d4` |
| 非法输入不创建快照 | 通过 | 倒置里程任务 `BLOCKED`；确认 HTTP 400；快照数保持 1 |

## 3. 网页闭环

通过真实 HTTP 管理 API 上传 XLSX、DOCX、专用映射 ID 和复核决定。转换任务 `CONV-20260826T023451Z-6b2d9196` 达到 `READY_FOR_CONFIRMATION`；重复提交被去重并返回同一任务；确认后创建不可变快照 `SNAP-b1cfcc71-3f9e-4c67-8b03-05de5396a798`。快照中的 payload 哈希、映射 ID/版本/哈希、转换器版本、源文件哈希、确认人和确认时间均已固化在本机验收数据库。

负向案例把起终里程改为 1+000 → 0+000。任务 `CONV-20260826T023451Z-c37af102` 被转换质量和输入合同阻断，确认接口返回 HTTP 400，未产生快照。

## 4. 软件与回归

- 平台包版本：0.9.0；转换器版本：0.3.0；
- 新能力：多行/合并式表头、单线精确行过滤、受控一表多目标映射、显式单位后缀解析；
- 新配置：`jiangxi-natural-gas.jiujiang.v1` v1.0.0；
- 新脱敏黄金回归：源 CSV + 期望业务 JSON；
- 架构检查：`ARCHITECTURE CHECK PASSED`；
- 自动化测试：`Ran 106 tests`，`OK`。

## 5. 阶段门结论

内部技术验收结论为 `PASSED_INTERNAL_FUNCTION_VALIDATION`。除业务签批外，11.2 的转换功能和数据门技术标准均有证据通过。

当前范围结论为 `PASSED_INTERNAL_FUNCTION_VALIDATION`，阶段 1 功能验证完成。根据项目方 2026-08-26 决定，业务数据负责人签批暂不执行，正式 G1 记为 `DEFERRED_NOT_IN_CURRENT_FUNCTION_VALIDATION_SCOPE`，待进入工程验证或正式发布范围后恢复。该延后决定不能由主要开发/执行主体自审替代，也不构成正式数据验收通过。
