# 阶段 1 需求、问题与风险状态更新

## 1. 需求更新

| 编号 | 阶段 1 状态 | 证据/说明 |
|---|---|---|
| REQ-003 | 当前功能范围满足 | 黄金候选只采用 JJ001～JJ041G 全线边界；正式业务确认延后 |
| REQ-004 | 技术满足 | 42 条字段来源和完整转换/复核审计 |
| REQ-005 | 满足 | 精确行过滤；芦溪行在 6 个业务视图中均被排除 |
| REQ-006 | 满足 | 真实案例 0 人口单元；空缺保持 `MISSING` |
| REQ-007 | 技术满足 | DOCX 辅助内容有复核决定、时间、原因和审计 |
| REQ-012 | 满足 | `jiangxi-natural-gas.jiujiang.v1` v1.0.0 已建立并回归 |
| REQ-013 | 当前功能范围满足 | 黄金候选已生成；业务数据负责人正式确认延后至工程验证/发布前 |
| REQ-014 | 满足 | CLI/Web 深度一致；重复哈希一致；负向门禁通过 |

## 2. 问题更新

| 编号 | 更新状态 | 说明 |
|---|---|---|
| ISS-003 | 已缓解，内部细分证据仍开放 | 黄金候选不再使用 11 段照片比例里程；如需内部管段仍须权威桩号/测绘数据 |
| ISS-004 | 当前功能范围关闭 | 已有专用映射、黄金候选、真实网页任务和快照；正式业务确认延后 |
| ISS-005 | 开放 | 壁厚、温度、阀门里程仍缺失，不属于忠实转换可自动补值范围 |
| ISS-007 | 开放 | 人口坐标、昼夜和室内外比例未补齐；转换结果明确不生成计算人口 |
| ISS-009 | 内部技术关闭 | 隔离验收数据库已有真实转换任务和不可变快照；不等于生产数据库上线 |
| 新增 ISS-011 | 延后、当前不阻断 | 业务数据负责人正式签批移至工程验证/发布范围；恢复正式 G1 时重新打开 |

## 3. 风险更新

| 编号 | 更新状态 | 当前控制 |
|---|---|---|
| R-01 资料不完整 | 仍开放 | 缺失显式化；不生成无证据人口和压力单值 |
| R-02 多管线错误关联 | 技术验证已缓解 | `record_filters` + 脱敏/真实负向与回归证据 |
| R-03 临时里程被当事实 | 已显著缓解 | 黄金候选改为台账全线单段；11 段只保留历史对照 |
| R-07 敏感资料泄露 | 持续控制 | 真实源、SQLite、黄金候选和报告只在 `workspace/runtime`；Git 仅保存脱敏夹具 |
| R-12 媒体无坐标/时间 | 仍开放 | 0/57 EXIF/GPS/时间已核实；媒体只作路线审计证据 |

## 4. 新决定

`D-009`：阶段 1 黄金候选采用台账直接证明的 0～10.938 km 单段，不沿用按照片数量比例换算的 11 段里程。后续取得权威内部里程或坐标时，创建新映射、新黄金 JSON 和新快照，不覆盖当前证据。

`D-010`：运行压力 `3.0-3.8 MPa` 在转换层保持范围文本；阶段 2/3 如需单值或分布，必须由模型/数据负责人另行批准，不能写入阶段 1 映射默认值。

`D-011`：内部技术确认可以生成受控快照，但 G1 在业务数据负责人签批前保持 HOLD。该决定已被 D-012 调整。

`D-012`：项目方于 2026-08-26 明确当前只验证功能是否实现，业务数据负责人签批暂时跳过。阶段 1 按内部功能验证范围记为完成，正式 G1 状态改为 `DEFERRED_NOT_IN_CURRENT_FUNCTION_VALIDATION_SCOPE`；进入工程验证或正式发布范围时恢复签批和正式 G1，当前结果不得描述为正式数据验收或正式发布。
