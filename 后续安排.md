你下一步最应该做的不是继续增加算法或美化报告，而是：

> 先补齐“原始资料 → 人工复核 → 输入快照”这个断点，并用一套真实但可控的资料跑通完整闭环。

因为现在后半段“标准JSON→计算→报告”已经能工作，真正卡住整套产品的是第一部分无法稳定把原始资料变成可确认的数据。

# 推荐工作路线

## 第0阶段：先接管和冻结当前版本

时间：2～3天。

当前代码库有大量未提交修改和新增文件。继续开发前必须先建立一个可回退的接管基线。

要做：

1. 备份当前SQLite数据库、OCR配置和运行产物。
2. 记录当前代码状态和依赖版本。
3. 把当前能运行的版本形成一个基线分支或标签。
4. 保存当前204项测试结果。
5. 列出当前所有页面、接口、数据库表和启动命令。
6. 明确哪些文件是上一任留下的有效修改，哪些只是临时文件。

验收标准：

- 换一台机器或新目录能够重新启动系统；
- 当前2份快照、7个任务和报告不会丢；
- 后续修改失败时可以恢复到当前状态。

---

## 第1阶段：确定一个真实试点，不要一开始追求“任意资料”

时间：2～3天。

第一版不要承诺“所有图片、所有报告、所有表格都能自动识别”。先选择一个明确试点，例如：

- 一条天然气支线；
- 10～20个管段；
- 5～10份资料；
- 明确资料类型；
- 明确需要抽取的字段；
- 明确最终只做筛查报告还是完整QRA。

建议第一批资料限定为：

- 管段台账Excel
- 运行工况Excel/CSV
- CIPS检测表
- 缺陷/维修记录
- 人口或受体表
- 1～2份扫描PDF或图片

同时建立“输入资料—目标字段—计算节点”的清单：

| 资料 | 要抽取的字段 | 影响哪个计算节点 |
|---|---|---|
| 管段台账 | 管径、壁厚、起止里程、坐标 | 管段几何、源项 |
| 运行工况 | 压力、温度、组分 | 泄漏源项、后果 |
| CIPS | 保护电位、异常比例 | 失效频率修正 |
| 缺陷记录 | 缺陷类型、尺寸、修复状态 | 失效频率 |
| 人口资料 | 位置、人数、昼夜分布 | IR、F-N、PLL |

没有这张清单，大模型提取出来再多文字也不知道是否对计算有用。

---

# 第2阶段：优先开发第五阶段“人工复核工作台”

时间：2～3周。

这是当前最重要的开发任务。

现在系统要求用户粘贴 `review_decisions JSON`，普通用户无法使用。需要做成真正的业务页面。

## 页面至少要实现

### 左侧：待复核字段列表

显示：

- 字段名称
- 大模型提取值
- 置信度
- 是否阻断计算
- 影响哪些节点
- 问题类型：缺失、冲突、低置信度、单位异常等

### 中间：候选值比较

同一个字段可能来自多份资料，需要同时显示：

- 候选值A、B、C
- 来源文件
- 页码、表格、单元格或图片坐标
- 原始文本
- 模型版本
- 提取方法
- 置信度

### 右侧：原始证据

用户点击字段后，直接看到：

- 原Excel单元格
- PDF页码
- 图片框选区域
- OCR原文
- 原始文件信息

### 业务操作

每个字段提供：

- 接受候选值
- 手工修改
- 选择另一个来源
- 驳回
- 标记不适用
- 请求重新提取
- 填写确认原因

### 最终门禁

确认前显示：

- 仍有多少阻断问题
- 哪些计算节点可以运行
- 哪些节点会因缺数据跳过
- 形成快照后的字段数量
- 数据和决定哈希
- 确认人、时间和理由

只有无阻断问题时才能创建快照。

## 阶段验收标准

- 业务人员完全不需要写JSON；
- 所有关键字段都能定位到原始证据；
- 冲突不能被系统静默决定；
- 所有手工修改都有审计记录；
- 相同输入和相同复核决定生成相同快照哈希；
- 已确认快照不能原地修改。

---

# 第3阶段：修复OCR和大模型真实资料鲁棒性

时间：与复核工作台并行，约2周。

当前6 MB图片被百炼以“请求体过大”拒绝，这是一个明确的P0问题。

## 必须处理

1. 上传限制和模型限制保持一致。
2. 大图片调用模型前自动缩放或压缩。
3. 超长图片自动切片，保留切片与原图坐标映射。
4. 扫描PDF按页处理，不能一次发送整个文件。
5. 表格区域和正文区域可分别识别。
6. 请求过大时自动降级，而不是直接结束整个任务。
7. 失败调用也必须进入模型调用审计。
8. OCR成功但字段提取失败时，应保留OCR结果供人工复核。
9. 支持对单个文件、单页或单字段重新提取。

## 建立真实资料黄金集

至少准备20～50份脱敏资料，由QRA业务人员人工标注正确答案。

建议验收指标：

- 关键字段证据绑定率：100%
- 未知字段或无证据候选：必须拒绝
- 关键字段精确率：建议不低于95%
- 关键字段召回率：建议不低于90%
- 冲突识别率：100%
- 不允许把空白自动变成0
- 不允许猜测坐标系、单位或日期
- 大模型提示注入不能改变工作流

具体阈值最终应由业务负责人和QRA专家批准。

---

# 第4阶段：完成第一部分到第二部分的真实闭环

时间：1～2周。

这一阶段不要用直接上传JSON来代替验收，必须从原始文件开始。

完整验收路径应当是：

```text
上传原始资料
→ 文件安全检查
→ OCR/文档解析
→ 大模型提取
→ 标准化与冲突检测
→ 业务人员页面复核
→ 确认生成快照
→ 自动创建计算任务
→ 产生管段风险结果
→ 打开完整报告
```

数据库必须出现：

- `candidate_field > 0`
- `candidate_evidence_link > 0`
- `extracted_entity > 0`
- `input_snapshot_provenance > 0`
- 转换任务状态为 `CONFIRMED`
- 快照能够反查转换任务和原文件
- 计算任务能够反查该快照

这才算第一部分和第二部分真正接上了。

建议把这个里程碑命名为：

> M1：原始资料到筛查报告闭环

---

# 第5阶段：补齐第二部分数据，让计算从5/11提升到11/11

时间：4～8周以上，主要取决于数据和QRA专家。

当前最新任务只完成5个节点。需要针对6个跳过节点逐项补数据。

## 优先补充

1. 正式失效频率库。
2. 分机理孔径比例。
3. 经批准的修正系数或校准模型。
4. 运行压力、温度和天然气组分。
5. 壁厚、管径、阀室及上下游隔离信息。
6. 泄漏点离散参数。
7. 气象联合概率。
8. 点火源分布。
9. 人口和受体空间坐标、昼夜人数。
10. 建筑物和室内外暴露修正。
11. 真实GIS中心线。

## 还需完成的算法验证

- 瞬态完全破裂泄放与管存衰减
- 高压气体近场扩散
- TNO曲线数字化独立校核
- IR网格收敛
- 泄漏点间距收敛
- 外部软件或独立案例对比
- 风险接受准则审批

## 分两级验收

### 筛查版

允许部分节点完成，但必须：

- 明确使用先验或默认值；
- 输出上下界；
- 显示补数清单；
- 禁止正式签发；
- 不把缺失值显示为0。

### 完整QRA版

要求：

- 11/11节点完成；
- 无关键输入缺失；
- 生产参数库已批准；
- 外部基准验证通过；
- IR和F-N可计算；
- 风险准则已经批准；
- QRA专家完成独立复核。

建议把这个里程碑命名为：

> M2：真实数据完整QRA计算验收

---

# 第6阶段：再建设第三部分报告智能体

时间：2～3周，在结果合同稳定后开始。

现在不应优先做报告大模型，因为上游数据和计算仍然不完整。否则只会让大模型包装一个不完整结果。

## 报告智能体的正确边界

它只能读取：

- 已冻结的输入快照
- 已冻结的计算结果
- 节点状态
- 图表
- 证据索引
- 模型和参数版本

它不能：

- 修改PLL、IR或F-N数值；
- 删除不利场景；
- 自己补充缺失工程数据；
- 自行决定风险可接受；
- 把筛查结果写成正式结论。

## 建议采用两层结构

### 大模型层

生成结构化报告草稿：

- 项目摘要
- 数据完整度说明
- 主要风险管段
- 风险驱动因素
- 不确定性说明
- 缺失数据
- 建议补充资料
- 使用边界

每句话必须引用对应结果或证据编号。

### 确定性排版层

程序负责：

- 把真实数值填入固定位置；
- 自动核对报告中的数字；
- 生成图表；
- 输出HTML、DOCX或PDF；
- 生成引用目录；
- 保存报告版本和哈希。

## 验收标准

- 报告中全部数字与计算结果逐项一致；
- 每个结论都有来源；
- 大模型不能修改数值；
- 缺失项不能被写成已完成；
- 筛查报告和正式报告模板严格分开；
- 报告必须经过人工复核和签发。

里程碑可命名为：

> M3：受控报告智能体与人工签发

---

# 第7阶段：生产化

时间：2～4周以上。

当业务闭环稳定后再做：

- 企业统一登录
- 角色权限：上传人、复核人、QRA专家、管理员、审计人
- HTTPS
- 数据库备份恢复
- 任务队列和并发控制
- 超时、重试和告警
- 大模型费用统计
- 数据外发审批
- 日志脱敏
- 项目级数据隔离
- 报告审批和电子签发
- 灾难恢复和运维文档

---

# 推荐时间安排

| 时间 | 主要工作 | 里程碑 |
|---|---|---|
| 第1周 | 冻结基线、选择试点、确定字段与资料清单 | 接管完成 |
| 第2～4周 | 复核工作台、OCR大文件修复、单文件重提取 | 第一部分可由业务人员操作 |
| 第5～6周 | 真实资料黄金集、端到端闭环 | M1：原始资料到筛查报告 |
| 第7～12周 | 补数据、参数库、算法验证、11/11节点 | M2：完整QRA验收 |
| 第10～13周 | 报告智能体、引用核验、DOCX/PDF | M3：受控报告生成 |
| 第13周以后 | 权限、部署、备份、监控和试运行 | 生产试点 |

如果只有一个开发人员，时间可能需要翻倍；如果有4～6人的小组，上述安排较现实。

# 人员分工建议

- 产品/项目负责人：确定试点范围、验收标准和优先级。
- QRA专家：确认字段、参数库、计算模型和风险准则。
- 数据/大模型工程师：OCR、提取、黄金集和准确率评估。
- 后端工程师：任务、复核、快照、审计和模型调用。
- 前端工程师：复核工作台、证据查看和结果交互。
- 测试人员：端到端、黄金回归、失败路径和报告一致性。

最优先的三个具体任务是：

1. 冻结和备份当前版本。
2. 开发不需要手写JSON的可视化复核工作台。
3. 用一套真实试点资料证明“原始文件→确认快照→计算→报告”能够完整走通。

在这三件事完成前，暂时不要继续扩展更多算法页面或报告样式。